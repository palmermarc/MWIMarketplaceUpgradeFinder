'use client';

import { GatheringCalculator } from '@/components/GatheringCalculator';

export function SkillsLevelingCalculator() {
  return (
    <div className="w-full">
      <GatheringCalculator />
    </div>
  );
}